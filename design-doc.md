# 在庫・見込み管理アプリ 設計ドキュメント

最終更新：2026年8月

---

## 1. アプリの目的

- 在庫管理から、製造依頼数の管理につなげる
- 各担当者が前年数量（得意先・アイテム別）を見ながら、見込み数（フォーキャスト）を入力
- 全体を管理者が集約・確認する
- **欠品防止**

---

## 2. 利用者・規模

- 見込み入力する担当者：全国の営業 約30人
- 認証：簡易パスワード（社内システムとの連携なし）
- 利用環境：基本は社内PC。外出先アクセスは「あれば良い」程度（必須ではない）
- ホスティング：社内サーバーに設置予定

---

## 3. データベース設計

DB：PostgreSQL　管理ツール：DBeaver　DB名：`Hanbai_zaiko`

### 3.1 items（アイテムマスタ）

| 項目名 | 型 | 説明 |
|---|---|---|
| item_id | VARCHAR(20) PK | 社内商品CD |
| item_name | VARCHAR(100) | 商品名 |
| case_qty | INTEGER | ケース入数（見込み・出荷数の単位） |
| jan_code | VARCHAR(13) | JANコード（8桁・13桁どちらも許容、可変長） |

### 3.2 customers（得意先マスタ）

| 項目名 | 型 | 説明 |
|---|---|---|
| customer_id | VARCHAR(20) PK | 荷送付先CD（下層CD） |
| customer_group_id | VARCHAR(20) | 得意先CD（上位階層、約500件。名前などの独自情報は持たせない） |
| customer_name | VARCHAR(100) | 得意先名 |
| obic_code | VARCHAR(20) | OBIC側の得意先コード |
| staff_id | VARCHAR(20) | 担当者CD（1得意先＝1担当者） |
| department_code | VARCHAR(20) | 部署CD（例：東京家庭用、東京業務用など。支店より細かい単位。計8つ想定） |

### 3.3 staff（担当者マスタ）

| 項目名 | 型 | 説明 |
|---|---|---|
| staff_id | VARCHAR(20) PK | 担当者ID |
| staff_name | VARCHAR(50) | 担当者名 |
| password | VARCHAR(255) | ハッシュ化パスワード |
| role | VARCHAR(10) | staff / admin |

### 3.4 stock（在庫データ、会社システムから毎日CSV取込）

| 項目名 | 型 | 説明 |
|---|---|---|
| date | DATE | データ取得日 |
| item_id | VARCHAR(20) FK→items | アイテム |
| stock_qty | INTEGER | 在庫数 |
| warehouse | VARCHAR(50) | 在庫場所（全国3拠点） |
| expiry_date | DATE | 賞味期限（ロット管理のため） |

PK: (date, item_id, warehouse, expiry_date)

### 3.5 shipments（出荷実績）

| 項目名 | 型 | 説明 |
|---|---|---|
| date | DATE | 出荷日 |
| item_id | VARCHAR(20) FK→items | アイテム |
| customer_id | VARCHAR(20) FK→customers | 荷送付先CD |
| shipped_qty | INTEGER | 出荷数 |

PK: (date, item_id, customer_id)

### 3.6 item_history（商品コード対応表）

季節商品（例：冷製スープ系）は、毎年商品CD（item_id）が変わる。前年比較のために、新旧の商品CDを紐付ける対応表。あなたが年次で手動管理する想定。

| 項目名 | 型 | 説明 |
|---|---|---|
| old_item_id | VARCHAR(20) FK→items | 去年の商品CD |
| new_item_id | VARCHAR(20) FK→items | 今年の商品CD |

PK: (old_item_id, new_item_id)

**前年比較のロジック**：今年のitem_idについて、まずこの対応表で`new_item_id`に一致する行を探す。見つかれば`old_item_id`を使って前年データを取得。見つからなければ、同じitem_idのまま前年データを取得（定番商品はCDが変わらないため）。

複数年（一昨年以前）まで遡る必要はない（フォーキャスト目的のため1年分で十分）。

### 3.7 forecasts（見込み入力）

| 項目名 | 型 | 説明 |
|---|---|---|
| date | DATE | 出荷予想日 |
| item_id | VARCHAR(20) FK→items | アイテム |
| customer_id | VARCHAR(20) FK→customers | 荷送付先CD |
| staff_id | VARCHAR(20) FK→staff | 入力した担当者 |
| forecast_qty | INTEGER | 見込み数 |

PK: (date, item_id, customer_id, staff_id)

---

## 4. データ取込

- 会社システム（OBIC）からCSVで抽出
- 将来的にAPI連携できるか要確認
- 保存期間：物理削除はせず、表示範囲の初期値を直近1年程度に絞る想定

---

## 5. UI設計

### 5.1 画面構成

- ログイン画面（ID・パスワードのみ）
- タブ1：メイン画面（全員共通）
- タブ2：全体確認（仮）（全員閲覧可）

### 5.2 タブ1：メイン画面

**上部：2軸メイングラフ**
- 左軸：在庫数（面グラフ）※数百単位
- 右軸：出荷実績／フォーキャスト（折れ線）※数十単位
- 横軸：日付（日／週／月で切り替え可）
- 「今日」を境に、過去＝出荷実績、未来＝前年実績とフォーキャストの2本に分岐
- 欠品まであと◯日のバッジを表示（在庫がゼロを割るタイミングの警告。アイテム単位のみ、得意先別のバッジはなし）

**下部：得意先一覧**
- 左：フィルター（得意先。自分の担当得意先のみ選択可）
- 右：表示切替タブ
  - デフォルト＝スモールグラフ（得意先ごとのカード、実績・前年・フォーキャストの3本を表示）
  - 切替＝テーブル形式（得意先名・アイテム名・前年実績・フォーキャスト列、数値はヒートマップ色付け、ソート・検索可）

**編集範囲**：自分の担当得意先のみ

**入力方法**
- スモールグラフのカード（またはテーブルの行）をクリック
- 画面に覆いかぶさるモーダルが開く（別ウィンドウではない）
- モーダル内で「出荷予想日」をカレンダーで選択（月単位ではなく日付固定）
- 「前年同日実績」を表示しながら、フォーキャスト数値を入力
- 補助機能：「この日以降は全てゼロにする」チェックボックス
  - 終売・取扱終了などで、以降の出荷予定がなくなった場合の一括処理
  - チェックして保存すると、選択日の翌日からデータ保持期間の終わり（直近1年程度）まで、フォーキャストを一括でゼロ更新
  - 元に戻す一括機能はなし。再開する場合は個別の日付を手動で編集
- 保存

### 5.3 タブ2：全体確認（仮）

- 全員（staff_idを持つ全員）が閲覧可能
- 上部：タブ1と同じ2軸メイングラフ＋欠品バッジ（絞り込み範囲で集計）
- 下部左：フィルター（部署・担当者・得意先、すべて選択可）
- 下部右：スモールグラフ／テーブル切替（タブ1と同じ部品）
- **CSV出力**：現在の絞り込み範囲のデータを出力
  - 出力列：得意先名・アイテム名・出荷予想日・フォーキャスト数値・前年実績
  - 全員が利用可能
- 編集：不可（adminロールのみ、編集ボタンが表示され、誰の担当分でも編集可能）

### 5.4 権限まとめ

| ロール | タブ1編集範囲 | タブ2閲覧 | タブ2編集 | CSV出力 |
|---|---|---|---|---|
| staff（一般担当者） | 自分の担当得意先のみ | 可 | 不可 | 可 |
| admin（複数人可、権限差なし） | 自分の担当得意先のみ | 可 | **可（全社対象）** | 可 |

---

## 6. 開発環境

- Node.js / VS Code / Git / PostgreSQL / DBeaver
- コード管理：GitHub（リポジトリ名 `hanbai_zaiko`）
- 開発拠点：会社PC・自宅PCの2台。GitHub経由で同期
- Node.js側のDB接続：`pg`ライブラリを使用（接続確認済み）

---

## 7. 未確定・today's TODO

- 会社PC側：フォルダ名統一、GitHubからclone、DB再作成（JANコード・得意先グループCD・賞味期限の反映）
- グラフの配色（過去／未来の色分け）は実装時に画面上で比較して決定
- 会社システム（OBIC）のデータ取込方法（CSV／API）を管理者に確認
- 外出先アクセスが必要になった場合のVPN等の検討
